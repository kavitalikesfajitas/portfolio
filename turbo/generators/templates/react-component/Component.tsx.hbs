import React from "react";
import clsx from 'clsx';

type {{pascalCase name}}Props = React.ComponentProps<"div">;

export function {{pascalCase name}}({ className, ...rest }: {{pascalCase name}}Props) {
  return (
    <div className={clsx(className)} {...rest}>
      Start working here
    </div>
  );
};